## TF - Tarefa Final - Aula 13

### Disciplina: Implementação de servidor e nuvem (cloud)
### Aula: 13 - Deploy de Containers na AWS com EKS
### Objetivo do TF: Avaliar a compreensão do fluxo completo de deploy de containers no AWS EKS (Elastic Kubernetes Service) e a proficiência na orquestração com Kubernetes.

### Orientações:

1. Responda às questões teóricas com base no conteúdo da aula.
2. A Tarefa Prática exige conhecimento dos comandos AWS CLI e kubectl utilizados no Lab013.
3. A Tarefa de Evidências exige a execução do `Lab013.md` e a coleta de prints/capturas de terminal que comprovem o fluxo completo.
4. As respostas devem demonstrar compreensão dos conceitos de Kubernetes, EKS e orquestração de containers.
5. **Prazo de entrega: 16/06/2026 às 23:59.**

## Questão 1: Conceitos de Kubernetes e EKS (Teórica)
O Amazon EKS (Elastic Kubernetes Service) é o serviço de Kubernetes gerenciado da AWS.

a) Qual é a diferença entre o **Control Plane** e os **Worker Nodes** em um cluster EKS? Quem gerencia cada um?

b) Explique o conceito de **self-healing** no Kubernetes. O que acontece quando um pod falha?

## Questão 2: Objetos Kubernetes (Teórica)
No Kubernetes, a aplicação é descrita através de manifestos YAML.

a) Qual é a diferença entre um **Deployment** e um **Service** no Kubernetes?

b) Explique o que são **Labels** e **Selectors** e como eles conectam um Service aos Pods corretos.

c) Qual é a função das **Probes** (livenessProbe e readinessProbe)? Dê um exemplo prático de quando cada uma é útil.

## Questão 3: IAM e Permissões (Teórica)
A segurança no EKS depende de IAM Roles corretamente configuradas.

a) Por que o cluster EKS precisa de uma IAM Role própria (`EKSClusterRole`)? Que tipo de ações essa role permite?

b) Por que os Worker Nodes precisam da política `AmazonEC2ContainerRegistryReadOnly`? O que acontece se essa política não estiver anexada?

## Questão 4: Networking e Exposição (Teórica)
A exposição de aplicações ao mundo externo no EKS envolve Services e Load Balancers.

a) Qual a diferença entre os tipos de Service `ClusterIP`, `NodePort` e `LoadBalancer` no Kubernetes?

b) O que acontece na AWS quando você cria um Service do tipo `LoadBalancer`? Qual recurso AWS é provisionado automaticamente?

c) Por que é importante deletar o Service LoadBalancer **antes** de deletar o cluster EKS?

## Questão 5: Tarefa Prática - Comandos EKS (Simulação)
Você precisa fazer deploy de uma aplicação no EKS. Assuma os seguintes valores:

* **Cluster:** `producao-cluster`
* **Namespace:** `minha-app`
* **Imagem ECR:** `111222333444.dkr.ecr.us-east-1.amazonaws.com/web-app:v2.0`
* **Replicas:** 3

### Descreva os comandos para:

a) **Criar o namespace** no cluster.

b) **Criar um Deployment** com 3 réplicas usando a imagem acima (pode descrever o YAML ou o comando).

c) **Expor a aplicação** ao mundo com um Service LoadBalancer na porta 80.

d) **Escalar** o deployment de 3 para 5 réplicas usando kubectl.

e) **Verificar** se todos os pods estão rodando e obter o endpoint do LoadBalancer.

## Questão 6: Evidências Práticas da Execução do Lab013
Você deve executar as etapas do arquivo `Aula 013/Lab013.md` e coletar evidências claras da execução.

### Parte 1: Construção e Teste Local
Forneça prints ou capturas de terminal de:

1. **Build da imagem Docker:** comando `docker build --no-cache -t ads-unifaat-site:v1.0 .` com saída de sucesso.
2. **Teste local:** resultado do `curl -s http://localhost:3000 | head -5` mostrando o HTML da página do curso de ADS.
3. **Listagem da imagem:** `docker images | grep ads-unifaat` mostrando a imagem construída.

### Parte 2: Publicação no ECR
Forneça prints ou capturas de terminal de:

1. **Login no ECR:** mensagem `Login Succeeded`.
2. **Push da imagem:** comando `docker push` mostrando o upload dos layers.
3. **Verificação remota:** `aws ecr describe-images` mostrando a tag `v1.0` no repositório.

### Parte 3: Cluster EKS e Deploy
Forneça prints ou capturas de terminal de:

1. **Cluster ativo:** `aws eks describe-cluster --name cluster-eks-ads --query 'cluster.status'` retornando `"ACTIVE"`.
2. **Nodes prontos:** `kubectl get nodes` mostrando 2 nodes com status `Ready`.
3. **Deploy aplicado:** `kubectl get deployments -n ads-unifaat` mostrando 2/2 replicas disponíveis.
4. **Pods rodando:** `kubectl get pods -n ads-unifaat` mostrando 2 pods com status `Running`.
5. **Service com endpoint:** `kubectl get svc -n ads-unifaat` mostrando o EXTERNAL-IP do LoadBalancer.

### Parte 4: Acesso e Testes
Forneça prints ou capturas de terminal de:

1. **Acesso via curl:** `curl -s http://$ENDPOINT | head -10` retornando o HTML da aplicação.
2. **Acesso via navegador:** screenshot da página do curso de ADS carregada no browser com a URL do LoadBalancer visível.
3. **Teste de resiliência:** evidência de deletar um pod e o Kubernetes recriar automaticamente (`kubectl delete pod` + `kubectl get pods -w`).

### Parte 5: Limpeza
Forneça prints ou capturas de terminal de:

1. **Verificação final:** saída do comando de verificação mostrando que clusters EKS, repositórios ECR e stacks CloudFormation foram removidos.

## Sessão de Entrega

> **⏰ Prazo: 16/06/2026 (terça-feira) às 23:59**

O aluno deve:

1. Fazer um fork deste repositório ou atualizar o fork feito na aula anterior.
2. Atualizar o repositório local se já estiver clonado.
3. Dentro da pasta `Aula 013` criar uma pasta com o seu RA.
4. Colocar dentro dessa pasta os artefatos criados: `README`, prints, capturas de terminal e evidências solicitadas.
5. Criar um `README` dentro da pasta do RA com:
   - as respostas das perguntas teóricas (Questões 1 a 5);
   - a lista dos comandos executados no Lab013;
   - a descrição de cada evidência coletada;
   - observações sobre qualquer erro ou diferença encontrada durante a execução.
6. Fazer um pull request com as alterações para o repositório do professor.
   - O título do pull request deve ser: `RA - Nome do Aluno`.

> **⚠️ Importante**
>
> - As respostas devem estar no `README` dentro da pasta com o RA do aluno.
> - Depois de atualizar o fork, crie a pasta do seu RA em `Aula 013` e adicione os artefatos antes de abrir o pull request.
> - Se os artefatos não estiverem na pasta `Aula 013`, haverá desconto na avaliação.
> - Se o título da PR não estiver no padrão `RA - Nome do Aluno`, haverá desconto na avaliação.
> - A limpeza dos recursos AWS é **obrigatória**. Evidencie a execução do cleanup.

## Como atualizar o fork feito anteriormente
Se você já possui um fork deste repositório, atualize-o antes de começar a tarefa:

1. Entre na pasta do repositório clonado.
2. Adicione o repositório original como `upstream` (se ainda não existir):
    ```bash
    git remote add upstream https://github.com/professor-ale/unifaat_implantacao_servidores-202601.git
    ```
3. Atualize o fork com os commits mais recentes:
    ```bash
    git fetch upstream
    git checkout main
    git merge upstream/main
    ```
4. Envie as alterações para o seu fork:
    ```bash
    git push origin main
    ```

> Após isso, crie a pasta do seu RA em `Aula 013`, adicione os arquivos e abra o pull request com o padrão de título exigido.
